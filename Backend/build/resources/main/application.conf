ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ uj.android.pprochot.ApplicationKt.module ]
    }
}
database {
    jdbcUrl = "jdbc:postgresql://localhost:5432/postgres"
    driverClassName = "org.postgresql.Driver"
    username = "shop_app"
    password = "shop"
}
